/** Matches `InstituteDashboard.jsx` chrome for pixel-consistent faculty UI. */
export const SIDEBAR_GOLD = '#1e4fa3'
export const SIDEBAR_GOLD_DARK = '#15397a'
export const ACTION_BLUE = '#1e4fa3'

/** Global « Back link — blue text, darker blue on hover (see `BackButton.jsx`). */
export const BACK_BUTTON_CLASS =
  'text-xs font-semibold uppercase tracking-wider text-[#1e4fa3] hover:text-[#15397a] transition-colors'
