-- study_queries was a legacy dashboard aggregate stub (0 rows, never read/written).
-- Dropped in favor of study_materials for faculty catalog material counts.
-- Safe to run when the table is empty or no longer needed.

DROP TABLE IF EXISTS public.study_queries;
